pre-commit:
  parallel: true
  commands:
    lint-staged:
      run: yarn lint-staged
    type-check:
      run: yarn type-check
      files: git diff --cached --name-only --diff-filter=ACMR | grep -E '\.(ts|tsx)$'

commit-msg:
  commands:
    commitlint:
      run: yarn commitlint --edit {1}
